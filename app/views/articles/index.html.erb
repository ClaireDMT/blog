<p id="notice"><%= notice %></p>

<h1>Articles</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td><%= article.title %></td>
        <td><%= article.body %></td>
        <td><%= link_to 'Show', article %></td>
        <td><%= link_to 'Edit', edit_article_path(article) %></td>
        <td><%= link_to 'Destroy', article, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= cl_image_tag("d4vhcx3obuf8br8zucgt",
      width: 400, height: 300, gravity: :face, crop: :thumb) %>

<!-- for face detection -->
<%= cl_image_tag("r82zjs1gxb2d5ipnot3b",
      width: 150, height: 150, crop: :fill) %>

<%= link_to 'New Article', new_article_path %>
